{
  "name": "cypress-typescript-bdd",
  "version": "1.1.0",
  "description": "Cypress + TypeScript + Cucumber BDD against DuckDuckGo",
  "private": true,
  "scripts": {
    "test": "cypress run --browser chrome --headless",
    "test:headed": "cypress open",
    "cypress:run": "cypress run --browser chrome --headless"
  },
  "author": "GitYourCodeOn",
  "license": "ISC",
  "devDependencies": {
    "@badeball/cypress-cucumber-preprocessor": "^22.2.0",
    "@cypress/webpack-preprocessor": "^6.0.4",
    "cypress": "^13.17.0",
    "ts-loader": "^9.5.2",
    "typescript": "^5.8.3",
    "webpack": "^5.101.0"
  },
  "cypress-cucumber-preprocessor": {
    "stepDefinitions": [
      "cypress/e2e/step_definitions/**/*.{js,ts}",
      "cypress/e2e/**/*.{js,ts}"
    ]
  }
}
